export const RISU_LUA_DEV_GUIDE: string = "================================================================\nRisuAI Lua + HTML/CSS 상태패널 개발 가이드\n실전에서 부딪힌 버그들로 정리한 \"처음부터 이렇게 짜라\"\n================================================================\n이 문서는 특정 캐릭터/세계관과 무관한 일반 가이드다.\n완전히 다른 시스템(전투 시스템, 인벤토리, 경제 시스템 등)을\n새로 만들 때도 아래 원칙은 그대로 적용된다.\n\n================================================================\nPART 1. RisuAI Lua 런타임의 특성 — 알아야 코드가 안 깨진다\n================================================================\n\n■ 1-1. 스크립트는 매 호출마다 처음부터 다시 실행된다\n────────────────────────────────────────────────────────\nRisuAI는 onStart / onOutput / onInput / editDisplay 콜백을 부를 때마다\nLua 파일 전체를 새로 실행하는 것에 가깝게 동작한다(실기기 관찰 기준).\n즉 스크립트 최상단의 `local CHARS = {}` 같은 변수는 매번 빈 상태로\n시작한다. 영속적인 데이터는 반드시 getState/setState로 다뤄야 하고,\n콜백이 호출될 때마다 \"state에서 복원 → 메모리 구조 재구축\"하는\n초기화 함수(ensureInit류)를 제일 먼저 실행해야 한다.\n\n  ❌ 잘못된 가정: \"한 번 등록한 캐릭터는 CHARS 배열에 계속 남아있다\"\n  ✅ 올바른 패턴: 매 콜백 시작 시 ensureInit(id) → state에서 복원\n\n■ 1-2. onInput은 사용자가 입력한 텍스트에 접근할 수 없다 (실기기 확인됨)\n────────────────────────────────────────────────────────\ngetChatLength/getChat이 onInput 시점엔 아직 그 메시지를 반영 안 한다.\n→ \"사용자가 특정 명령어를 입력하면 즉시 반응\" 같은 기능은 onInput으로\n  못 만든다. 대신:\n  - AI 응답(editDisplay 처리 대상)에서 감지 (AI가 그 명령을 따라 써야 함)\n  - 또는 onOutput에서 다음 AI 턴에 자동 보정하는 방식으로 우회\n\n■ 1-3. onStart는 새 채팅의 첫 메시지(유저가 붙여넣은 것)에 접근 가능 (실기기 확인됨)\n────────────────────────────────────────────────────────\ngetChat(id, 0)이 {role=, data=, time=} 형태로 첫 메시지를 돌려준다.\n이걸 이용하면 \"export 데이터를 새 채팅에 붙여넣으면 자동 복원\"같은\n기능을 onStart에서 구현할 수 있다. onInput과 정반대 특성이니 헷갈리지 말 것.\n\n■ 1-4. \"메시지가 안 바뀌면 editDisplay가 재실행되지 않는다\" (실기기 관찰)\n────────────────────────────────────────────────────────\n같은 텍스트를 setChat으로 다시 써도 RisuAI가 변경을 감지 못해\n화면이 갱신 안 되는 경우가 있었다. 해결: 매번 다른 텍스트로 만들어서\n\"확실히 바뀌었다\"는 신호를 준다 (예: 마커 뒤에 #턴번호를 붙여 캐시버스팅).\n\n  setChat(id, idx, \"<!--my-marker-->#\" .. tostring(getChatLength(id)))\n\n================================================================\nPART 2. Lua 문법 자체에서 터지는 문제 — 가장 흔하고 가장 황당한 버그\n================================================================\n이 파트는 런타임 로직과 무관하게, \"스크립트가 아예 로드조차 안 되는\"\n문법 단계 사고를 다룬다. 데이터 분량이 많은 캐릭터/아이템 정의\n테이블을 손으로 짜거나 복붙으로 늘릴 때 특히 잘 터진다.\n\n■ 2-1. 테이블 키에 비ASCII(한글 등) 문자를 따옴표 없이 쓰면 안 된다\n────────────────────────────────────────────────────────\nLua의 테이블 생성자 `{ key = value }`에서 key는 Lua의 \"이름(Name)\"\n토큰 규칙을 따라야 한다 — 영문자/숫자/언더스코어(_)만 허용되고,\n한글을 포함한 모든 비ASCII 문자는 식별자로 쓸 수 없다.\n\n  실제 사고 (두 번 따로 발생):\n    { 대딸=true, 펠라티오=true }        ← 순수 한글 키\n    { anon_현재남친 = {...} }            ← 영문 접두사 + 한글 혼합 키\n  둘 다 똑같이 깨진다. \"anon_\"까지는 영문이라 괜찮아 보이지만,\n  Name 토큰은 첫 글자부터 끝까지 전부 ASCII여야 하므로 소용없다.\n\n  ❌ local acts = { 펠라티오=true, 림잡=true }\n  ✅ local acts = { [\"펠라티오\"]=true, [\"림잡\"]=true }\n\n  반대로 따옴표로 감싼 \"문자열 값\"은 전혀 문제가 없다\n  (예: category = \"일반섹스\"). 문자열 리터럴 내부는 그냥 바이트\n  나열이라 Name 규칙이 적용되지 않는다. 헷갈리지 말 것 — 깨지는\n  건 \"따옴표 없는 키\" 뿐이다.\n\n  에러 메시지 특징: `unexpected symbol near '<\\xxx>'` 형태로 나오며\n  \\xxx는 한글 UTF-8 첫 바이트(보통 \\234~\\237 부근, 16진수 EA~ED대)다.\n  처음 보면 \"인코딩이 깨졌나\" 의심하기 쉽지만, 실제로는 한글 자체는\n  멀쩡하고 문법적으로 허용 안 되는 자리에 있을 뿐인 경우가 많다.\n  → 인코딩 문제와 식별자 규칙 문제를 구분하려면 2-3을 참고.\n\n■ 2-2. 중괄호 짝이 안 맞는 사고 — 비슷한 블록을 복붙으로 늘릴 때 제일 잘 남\n────────────────────────────────────────────────────────\n실제 사고: 캐릭터 데이터 배열(`local CHARS = { {...}, {...}, ... }`)에서\n두 번째 캐릭터 항목이 끝나는 자리에 군더더기 `}`가 하나 더 끼어들어\n배열 전체(CHARS)를 그 시점에서 조기 종료시켜버렸다. 그 뒤에 있던\n나머지 캐릭터 항목들은 배열 밖에 붕 뜬 채로 파싱되어 문법 오류가 났고,\n정작 배열의 진짜 끝에는 있어야 할 닫는 `}`가 빠져 있었다 — 괄호\n\"개수\"는 우연히 맞았지만 \"위치\"가 틀린, 눈으로 훑어서는 잘 안 잡히는\n유형이다.\n\n  ❌ 들여쓰기만 보고 \"짝이 맞겠지\" 판단 — 특히 사람이 손으로 짠\n     코드는 들여쓰기가 항목마다 미묘하게 다를 수 있어 신뢰할 수 없다.\n  ✅ luac -p가 가리키는 줄 번호를 기준으로, 그 지점 앞뒤의 `{`/`}`를\n     하나씩 세어 실제 중첩 깊이를 확인한다. 의심 가는 줄을 지워보고\n     재컴파일 → 다음 에러가 어디로 옮겨가는지 보면서 진짜 원인을\n     역추적하는 게 가장 빠르고 확실하다.\n\n■ 2-3. RisuAI가 보여주는 에러의 줄 번호를 곧이곧대로 믿지 마라\n────────────────────────────────────────────────────────\n같은 파일, 같은 에러인데도 RisuAI 앱이 보고한 줄 번호(예: 441, 557)와\n그 .lua 파일을 luac로 직접 컴파일했을 때 나온 진짜 줄 번호(예: 408)가\n서로 달랐다. RisuAI가 여러 스크립트(라이브러리/트리거 등)를 합쳐서\n실행하는 과정에서 줄 번호 오프셋이 생기는 것으로 보인다.\n\n  → RisuAI 에러 메시지의 줄 번호는 \"대략적인 단서\" 정도로만 쓰고,\n    정확한 위치는 항상 그 파일 단독으로 직접 컴파일해서 확인하라:\n      luac5.4 -p 파일.lua\n    (또는 Windows라면 lua.exe -e \"assert(loadfile('파일.lua'))\")\n  → 인코딩이 깨진 게 의심되면, 비ASCII 문자를 전부 지워보거나\n    바꿔보기 전에 먼저 luac로 검사해서 \"진짜 인코딩 문제인지\" 아니면\n    \"2-1 같은 문법 규칙 문제인지\"부터 구분하라. 후자인데 전자로\n    오인하고 애꿎은 문자들을 들어내면 증상이 안 없어지고 다른 줄로\n    옮겨갈 뿐이다.\n\n================================================================\nPART 3. 텍스트 마커 / 명령어 파싱 — 여기서 제일 많이 터졌다\n================================================================\n\n■ 3-1. \"메시지에 마커가 포함되어 있으면\" vs \"메시지가 마커 그 자체이면\"\n────────────────────────────────────────────────────────\n실제로 터진 사고: AI가 자기 <Thoughts> 안에서 \"유저가 <!--my-marker-->를\n보냈다\"라고 마커 문자열을 언급했는데, find()로 \"포함 여부\"만 검사하던\n코드가 AI의 17000자짜리 응답 전체를 패널 HTML로 통째로 갈아치웠다.\n\n  ❌ if data:find(\"<!%-%-marker%-%->\") then return buildPanel() end\n  ✅ if data:match(\"^%s*<!%-%-marker%-%->%s*$\") then return buildPanel() end\n\n→ 시스템 마커는 항상 \"메시지 전체가 마커 그 자체\"인 것만 매칭하라.\n  AI 응답 본문에 같은 텍스트가 우연히 등장해도 안전해야 한다.\n\n■ 3-2. 캐시버스팅 접미사를 붙였으면, 정확매칭 패턴도 그 접미사를 허용해야 한다\n────────────────────────────────────────────────────────\n3-1의 수정을 하고 나서 곧바로 새로운 버그가 났다: 마커에 \"#턴번호\"를\n붙이는 캐시버스팅(1-4 참고)을 하고 있었는데, 정확매칭 정규식이\n순수한 마커 문자열만 허용해서 \"#1\", \"#2\"가 붙은 순간부터 마커가\n인식 안 되고 패널이 통째로 사라졌다(텍스트 그대로 노출).\n\n  ❌ data:match(\"^%s*<!%-%-marker%-%->%s*$\")        -- 접미사 거부\n  ✅ data:match(\"^%s*<!%-%-marker%-%->#?%d*%s*$\")    -- 접미사 허용\n\n→ 정확매칭과 캐시버스팅은 같이 설계해야 한다. 둘 중 하나만 고치면\n  반드시 이런 회귀가 난다. 마커 형식을 바꿀 때마다 \"이 마커가 가질 수\n  있는 모든 변형\"을 한 번에 정의하고 테스트하라.\n\n■ 3-3. 키-값 파싱에서 구분자가 키 안에도 들어갈 수 있다는 걸 가정하라\n────────────────────────────────────────────────────────\n[entityKey]_[field]:value 형태의 동적 키 파싱에서, entityKey 자체에\n구분자(_)가 들어가는 경우(다단어 이름 등)를 놓치면 키가 잘못 잘린다.\n\n  실제 사고: \"스티브_트레버_vaginal\" 를 최소매칭으로 분리하면\n    pKey=\"스티브\", pType=\"트레버_vaginal\"  ← 완전히 틀림\n\n  ❌ k:match(\"^(.-)_(.+)$\")        -- 최소(non-greedy) 매칭, 첫 _ 에서 끊음\n  ✅ k:match(\"^(.+)_([^_]+)$\")     -- 마지막 _ 기준으로 분리\n\n→ \"구분자가 1개만 있을 것\"이라고 가정하지 마라. 키 자체가 사람이\n  자유롭게 짓는 이름(사람 이름, 한글 등)이라면 구분자가 여러 개\n  들어갈 수 있다는 전제로 정규식을 짜라.\n\n■ 3-4. 고정 필드 수(파이프 구분) 포맷은 입력자가 구분자를 안에 또 쓸 수 있다\n────────────────────────────────────────────────────────\nfield1|field2|field3 같은 고정 필드 포맷에서, 어떤 필드(예: \"허용 행위\n목록\")에 자연스럽게 \"|\"를 또 넣고 싶어지는 경우가 생긴다. AI가\n\"A|B|C.webp\" 형태로 3개를 의도했지만 실제로는 4필드로 파싱되어\n마지막 필드(이미지)가 엉뚱한 자리로 밀린다.\n\n  실전 대응 2가지:\n  a) 파싱 단계에서 \"필드 수 초과 시 마지막 필드를 고정 필드로,\n     중간 초과분을 자동 병합\"하는 방어 코드를 넣는다.\n  b) 프롬프트에 \"이 필드는 안에 구분자(|) 쓰지 말고 공백/슬래시로\n     구분하라\"는 명시적 경고 + 잘못된 예시/올바른 예시를 둘 다 보여준다.\n  → 가능하면 a+b 둘 다. 코드가 1차 방어, 프롬프트가 애초에 사고를 줄임.\n\n================================================================\nPART 4. 멱등성(Idempotency) — \"또 호출되면 어떻게 될까\"를 항상 따져라\n================================================================\n\n■ 4-1. \"추가\" 명령은 재호출 시나리오가 반드시 생긴다\n────────────────────────────────────────────────────────\nexport → 새 채팅에 붙여넣기 → 자동 복원(3-? 패턴) 구조를 쓰면,\n\"엔티티 추가\" 류 명령(part_add, rel_add, trait_add...)이 같은 데이터로\n여러 번 호출되는 게 일상이 된다. 이때 무조건 append만 하면 채팅을\n다시 불러올 때마다 같은 항목이 계속 쌓인다(중복 폭증).\n\n  ❌ table.insert(list, newItem)                         -- 항상 추가\n  ✅ 키로 기존 항목 검색 → 있으면 갱신, 없으면 추가\n\n■ 4-2. 그런데 \"갱신\"으로만 막으면 또 다른 사고가 난다\n────────────────────────────────────────────────────────\n4-1을 고치고 나서 \"같은 키면 완전히 무시\"로 막았더니, 이번엔 진짜\n정보 갱신(예: 캐릭터 이미지를 나중에 추가, 설명 보강)까지 막혀버렸다.\n\"새 정보가 와도 깡그리 무시당하는\" 사고.\n\n  ❌ if not exists then insert(item) end                 -- 완전 무시\n  ✅ if exists then 기본정보는 덮어쓰기, 진행상태는 보존\n     else 새로 추가\n\n→ 멱등성 처리는 단순 \"중복 방지\"가 아니라 \"재호출 시 무엇을 갱신하고\n  무엇을 보존할지\"를 필드 단위로 설계해야 한다. 일반적으로:\n    갱신해야 할 것  = 정적 속성 (이름/설명/이미지/외형)\n    보존해야 할 것  = 누적/진행 상태 (카운터, 히스토리, 호감도 등)\n\n■ 4-3. 멱등성 설계 체크리스트 (새 \"추가\" 명령을 만들 때마다)\n────────────────────────────────────────────────────────\n  □ 이 명령이 같은 키로 또 호출되면 무슨 일이 일어나야 하는가?\n  □ export → 재import 시나리오에서 의도한 대로 동작하는가?\n  □ \"갱신 목적 재호출\"과 \"단순 중복\"을 구분할 방법이 있는가?\n    (보통 구분할 필요 없음 — 같은 키면 항상 갱신하면 됨. 단,\n     진행 상태 필드는 항상 보존)\n  □ 테스트: 같은 메시지를 3번 연속 처리해도 카운트가 1로 유지되는가?\n\n================================================================\nPART 5. HTML/CSS 인라인 패널을 만들 때\n================================================================\n\n■ 5-1. 매 메시지마다 <style> 블록을 반복하지 마라\n────────────────────────────────────────────────────────\n긴 세션(수백 턴)에서 매 메시지가 자기만의 <style> 블록을 가지면,\nDOM에 동일한 CSS 규칙이 수백 번 중복 정의된다. 브라우저가 매번\n재파싱/재매칭해야 해서 스크롤 성능이 누적 저하된다.\n\n  대안 A) 정규식 치환(regex replace) 레이어를 쓴다면, AI는 가벼운\n          커스텀 태그(<talk>, <narration> 등)만 쓰고, 치환 결과에\n          필요한 만큼의 최소 CSS만 인라인으로 자급자족시킨다\n          (그 치환 1건이 의존하는 클래스만 <style>에 넣음 — 카드\n          전역 CSS 필드의 존재 여부를 가정할 수 없는 환경이라면\n          이 방식이 제일 안전하다).\n  대안 B) 호스트가 \"카드 CSS 필드는 전역 적용된다\"는 게 100% 확실하면\n          1회만 정의하고 메시지에는 클래스명만 쓴다. 단, 이 가정이\n          틀리면 스타일이 통째로 안 먹는다 — 검증 없이 단정하지 말 것.\n\n■ 5-2. CSS box-shadow/gradient/::before·after 의사요소는 비용이 크다\n────────────────────────────────────────────────────────\n대화창 하나당 box-shadow 2~3개 + linear-gradient + ::before/::after\n화살표 같은 구성을 메시지마다 반복하면, 메시지 수가 누적될수록\n리플로우/리페인트/GPU 합성 비용이 늘어난다. 디자인 톤은 유지하되\n의사요소 개수, 그림자 레이어 수를 최소화하는 게 장기 세션에 유리하다.\n\n■ 5-3. CSS 선택자: \"자손 결합자\"와 \"형제 결합자\"를 구조에 맞게 골라라\n────────────────────────────────────────────────────────\nradio-hack 방식 탭 전환(`#tab1:checked ~ .content1{display:block}`)을\n여러 레이어에서 쓸 때, DOM 구조가 레이어마다 다르면 같은 패턴의\n선택자를 복붙해선 안 된다.\n\n  실제 사고: 상위 탭은 `input → divA → divB(.pN 포함)` 구조라\n  `~div~div .pN`(자손 결합자, 2단계 아래)이 맞았는데, 그 안에 새로\n  만든 하위 탭(채팅방 전환)은 `input → .roomN`이 형제로 바로 붙는\n  구조라 같은 패턴을 복붙하니 전혀 매칭이 안 됐다.\n\n  ✅ 매번 실제 렌더링된 HTML을 디버그 출력해서 DOM 깊이를 확인하고,\n     그에 맞는 결합자(직계자손 `>`, 자손 ` `, 일반형제 `~`, 인접형제 `+`)\n     를 고른다. \"이전에 썼던 패턴이니 되겠지\"라고 추정하지 않는다.\n\n■ 5-4. 이미지가 컨테이너 비율과 안 맞을 때는 항상 object-position 명시\n────────────────────────────────────────────────────────\nobject-fit:cover만 쓰면 브라우저 기본값(center)에 의존하게 되는데,\n명시적으로 `object-position:center`를 같이 써서 의도를 분명히 하라.\n나중에 다른 정렬(top, 특정 좌표)이 필요한 이미지가 생겼을 때\n일괄 검색/치환이 쉬워진다.\n\n■ 5-5. 스크롤 가능한 콘텐츠 영역에는 항상 min-height:0을 같이 써라\n────────────────────────────────────────────────────────\nflex 컨테이너 안에 `overflow-y:auto; flex:1`인 자식이 있으면, 그\n자식에 `min-height:0`을 안 주면 콘텐츠 크기만큼 무한정 늘어나\n부모의 `overflow:hidden`에 잘려서 스크롤이 아예 안 먹는 고전적\nflexbox 버그가 난다. 매번 까먹기 쉬우니 \"스크롤 컨테이너 만들 때\n체크리스트\"에 고정으로 넣어라:\n  display:flex; flex-direction:column (부모)\n  flex:1; min-height:0; overflow-y:auto (스크롤 자식)\n\n■ 5-6. 긴 리스트 끝에는 spacer를 둬라\n────────────────────────────────────────────────────────\n스크롤 영역 마지막 항목이 컨테이너 하단 경계/UI 다른 요소에 가려\n보이는 문제가 반복적으로 났다. 리스트/로그 끝에 고정 높이\nspacer(`<div style=\"height:60px\"></div>`)를 넣는 것으로 간단히 해결.\n\n================================================================\nPART 6. AI에게 명령어를 가르칠 때 (프롬프트 설계)\n================================================================\n\n■ 6-1. \"커맨드가 있다\"는 것과 \"언제 써야 하는지\"는 별개로 가르쳐야 한다\n────────────────────────────────────────────────────────\n실제 사고: 카운터 갱신 커맨드의 문법은 프롬프트에 있었지만, AI가\n거의 안 썼다. 원인은 \"언제(트리거 조건)\" \"무엇이 어떤 카운터에\n해당하는지(행위→필드 매핑)\"가 빠져 있었기 때문. 문법만 있고 사용\n시점이 없으면 모델은 그 커맨드의 존재를 인지해도 실제로 쓰지 않는다.\n\n  프롬프트에 반드시 포함할 것:\n  a) 문법 (Format)\n  b) 트리거 조건 (WHEN — \"이 일이 일어나면 반드시\")\n  c) 행위→필드 매핑 (WHAT — 구체적인 동의어/상황 나열)\n  d) 절대값 vs 델타 명시 (HOW — \"현재값+1을 계산해서 적어라\" 같은\n     구체적 지시. \"누적\"이라는 단어만으로는 모호함)\n  e) 완전한 예시 (실제 숫자가 들어간 한 턴 전체 예시)\n\n■ 6-2. 동적으로 생성되는 키(사람이 지은 이름 기반)는 생성 규칙을 명시하라\n────────────────────────────────────────────────────────\n\"이 이름을 key로 변환해서 써라\" 같은 규칙이 코드에만 있고 프롬프트에\n없으면, AI는 그 key가 뭔지 추측만 하다가 결국 안 쓰거나 틀리게 쓴다.\n변환 규칙 + 구체적 변환 예시(한글 포함)를 프롬프트에 반드시 명시.\n엔진 쪽에서도 약간의 유연성(정확 키 + 원본 이름 둘 다 허용하는 fallback\n매칭)을 같이 넣으면 이중 안전장치가 된다.\n\n■ 6-3. 체크리스트는 본문 규칙과 분리해서 \"맨 위 한 곳\"에 모아라\n────────────────────────────────────────────────────────\n세부 규칙은 각 섹션에 길게 설명하되, \"매 턴 까먹기 쉬운 것들\"은\n프롬프트 맨 앞에 체크박스 리스트(PART 0 같은)로 압축 요약해서\n모아두면, 모델이 매 응답 생성 전에 빠르게 스캔하고 누락을 줄인다.\n체크리스트 항목이 본문 어느 섹션을 가리키는지 참조(예: \"(PART III-D)\")\n를 같이 적어 본문과 어긋나지 않게 유지하라.\n\n■ 6-4. \"이런 상황엔 정보를 지어내지 마라\"는 규칙은 예시로 못 박아라\n────────────────────────────────────────────────────────\n이미지 파일명처럼 \"존재 여부를 확신할 수 없으면 비워두라\"는 규칙은\n추상적으로 한 줄 적어두면 모델이 종종 무시하고 그럴듯한 파일명을\n지어낸다. \"불확실하면 필드를 비워두는 구체적 문법\"(예: 빈 파이프\n`||`)까지 보여주는 예시를 반드시 포함하라.\n\n================================================================\nPART 7. 디버깅 워크플로우 (이번 세션에서 실제로 썼던 방식)\n================================================================\n\n1. 사용자가 보고한 증상을 의심 없이 믿되, \"재현 가능한 최소 테스트\"를\n   먼저 만든다. 더미 데이터로 동일한 시퀀스를 lua5.1로 로컬 실행.\n2. 엔진의 핵심 함수(getState/setState/getChat/setChat/addChat/\n   listenEdit)를 가짜(stub)로 구현한 순수 Lua 테스트 하네스를 하나\n   만들어두고, 매 수정 때마다 누적해서 재사용한다(회귀 테스트 묶음).\n3. \"정상 동작\"과 \"재현된 버그\"를 모두 print로 비교 가능한 형태로\n   뽑는다 (예: #카운트, find 결과 true/false, 실제 렌더 일부 출력).\n4. 수정 → 같은 테스트 재실행 → 그 수정이 다른 케이스를 깨뜨리지\n   않았는지 기존 테스트 전체(회귀 셋)를 항상 같이 돌린다.\n5. luac -p로 문법 컴파일 체크는 매 수정마다 최우선으로 — 런타임까지\n   가기 전에 잡을 수 있는 오류는 그 단계에서 잡는다.\n6. luac는 만나는 첫 번째 문법 오류에서 멈춘다. 한 군데 고쳤다고\n   끝난 게 아니라, 에러가 완전히 사라질 때까지 \"수정 → 재컴파일\"을\n   반복한다. 한 파일에 같은 종류의 문법 오류가 여러 개 겹쳐 있는 게\n   드문 일이 아니다 (실제로 PART 2-1/2-2 사고가 같은 파일에 같이\n   있었다 — 첫 에러만 고치고 끝냈으면 두 번째에서 또 막혔을 것).\n7. 패턴성 버그(예: \"따옴표 없는 한글 키\")를 하나 발견했으면, 그\n   한 곳만 고치고 끝내지 말고 스크립트로 파일 전체를 스캔해서 같은\n   패턴이 몇 군데 더 있는지 먼저 확인한 뒤 한 번에 고친다. 그리고\n   수정 후에는 항상 diff로 \"의도한 곳만\" 바뀌었는지 확인한다 — 특히\n   정규식 기반 일괄 치환은 의도치 않은 줄까지 건드릴 수 있다.\n\n이 방식이 유효했던 이유: RisuAI 실기기 접근 없이도, \"엔진 로직 자체의\n정합성\"은 로컬 Lua 환경에서 거의 100% 사전 검증 가능했다. 실기기에서만\n드러나는 건 (a) Risu 고유 API 동작 방식(onInput 등 1장 내용)과\n(b) 브라우저 렌더링/CSS 이슈뿐이었다 — 이 둘은 처음부터 \"검증 안 된\n가정\"으로 표시해두고, 사용자 스크린샷으로 확인되는 즉시 가이드에\n박제하는 식으로 다뤘다.\n\n================================================================\nPART 8. 새 시스템을 시작할 때 — 처음부터 이렇게 설계하라\n================================================================\n\n  □ 모든 state는 getState/setState를 거친다. 전역 Lua 변수에\n    \"영속성\"을 기대하지 않는다.\n  □ 테이블 키로 한글 등 비ASCII 문자가 들어갈 수 있는 이름(사람 이름,\n    행위명 등)을 쓸 땐 처음부터 `[\"키\"]=값` 표기를 기본값으로 삼는다.\n    \"영문일 줄 알았는데 나중에 한글이 섞여 들어올\" 가능성을 미리 가정.\n  □ 캐릭터/아이템처럼 비슷한 구조의 데이터 블록을 복붙으로 늘릴 때마다\n    그 즉시 `luac -p`로 재검증한다. \"이전 블록이랑 똑같은 패턴이니\n    되겠지\"라고 넘어가지 않는다 (중괄호 짝 어긋남은 늘 이렇게 샌다).\n  □ 콜백 진입 시 항상 ensureInit(id)류 복원 함수를 제일 먼저 호출.\n  □ 시스템 마커(패널 트리거 등)는 \"메시지 전체 정확매칭\"으로 검사.\n    캐시버스팅 접미사를 쓸 거면 처음부터 그 접미사까지 패턴에 포함.\n  □ 동적 키 파싱은 \"구분자가 키 안에 또 나올 수 있다\"는 전제로 설계\n    (마지막 구분자 기준 분리, 또는 길이 prefix 등).\n  □ \"추가\" 계열 명령은 설계 단계에서 재호출 시나리오(특히 export/\n    import 왕복)를 표로 그려보고, 필드별로 갱신/보존을 결정한다.\n  □ HTML 패널은 처음부터 \"메시지당 비용\"을 의식해서 설계\n    (스타일 반복 최소화, 의사요소/그림자 절제, min-height:0 등\n    스크롤 체크리스트 기본 적용).\n  □ AI용 프롬프트는 새 커맨드 하나당 \"문법+트리거+매핑+예시\" 4종\n    세트를 기본으로 작성. 문법만 던지고 끝내지 않는다.\n  □ 로컬 Lua 테스트 하네스를 첫 기능 구현 때부터 같이 만들고,\n    이후 모든 수정에 회귀 테스트로 누적 재사용한다.\n";
